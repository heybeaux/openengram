# Phase 2 Exit-Gate Eval Results

_Generated by `scripts/eval/phase2-eval.ts` (EC-29)._

- Run time: 2026-05-25T16:23:45.526Z
- LLM adapter: `anthropic`
- Token budget per question: 8000
- Pass threshold per repo: 4/5

## Summary

| Repo | Passed | Avg tokens | Met threshold? |
|------|--------|-----------|----------------|
| `engram-code` | 5/5 | 748 | ✅ |
| `payments-app` | 5/5 | 553 | ✅ |

## `engram-code`

### engram-code-1-add-language — PASS ✅

**Question:** Where would I add support for a new source language (e.g. Rust) so it gets picked up by the indexing pipeline?

- Termination: `final_answer`
- Tokens used: 757
- Concept paths fetched: _(none)_
- Required hits: `engram-code/parsers`
- Reason: matched required conceptPath(s): engram-code/parsers

**Answer:**

```
engram-code/parsers — this subsystem contains the per-language tree-sitter extractors (currently TS, Python, Go), so adding a Rust extractor here is where the new language support would be implemented to feed into the indexing pipeline's Pass 1.
```

### engram-code-2-add-endpoint — PASS ✅

**Question:** Where would I add a new HTTP endpoint that returns gotchas for a given concept path?

- Termination: `final_answer`
- Tokens used: 745
- Concept paths fetched: _(none)_
- Required hits: `engram-code/api`
- Reason: matched required conceptPath(s): engram-code/api

**Answer:**

```
engram-code/api — The new HTTP endpoint returning gotchas for a given concept path belongs in the NestJS API subsystem under `src/v2/api/`, alongside the existing `cards`, `map`, `search`, and `subsystems` controllers.
```

### engram-code-3-add-pass — PASS ✅

**Question:** Where would I add a new synthesis pass — say, a "test-coverage" pass that summarizes test density per module?

- Termination: `final_answer`
- Tokens used: 747
- Concept paths fetched: _(none)_
- Required hits: `engram-code/passes`
- Reason: matched required conceptPath(s): engram-code/passes

**Answer:**

```
engram-code/passes — new synthesis passes (like a "test-coverage" pass that summarizes test density per module) belong in the passes subsystem, which is where all six existing synthesis passes live and produce LoD cards from structural input.
```

### engram-code-4-add-llm-provider — PASS ✅

**Question:** Where would I swap or extend the LLM provider — e.g. add a direct Anthropic SDK backend alongside OpenRouter?

- Termination: `final_answer`
- Tokens used: 748
- Concept paths fetched: _(none)_
- Required hits: `engram-code/llm`
- Reason: matched required conceptPath(s): engram-code/llm

**Answer:**

```
engram-code/llm — this subsystem contains the OpenRouter chat-completions client with retry + fallback, making it the right place to add a direct Anthropic SDK backend alongside (or instead of) the existing OpenRouter integration.
```

### engram-code-5-add-cli-command — PASS ✅

**Question:** Where would I add a new CLI subcommand, e.g. `engram-code subsystems` that lists detected subsystems?

- Termination: `final_answer`
- Tokens used: 741
- Concept paths fetched: _(none)_
- Required hits: `engram-code/cli`
- Reason: matched required conceptPath(s): engram-code/cli

**Answer:**

```
engram-code/cli — this subsystem is the command-line entry point where existing subcommands (index, cards, config) live, so a new `subsystems` subcommand belongs there.
```

## `payments-app`

### payments-app-1-add-provider — PASS ✅

**Question:** Where would I add a new payment provider — say, Braintree — so it can be used for checkouts?

- Termination: `final_answer`
- Tokens used: 549
- Concept paths fetched: _(none)_
- Required hits: `payments-app/payments`
- Reason: matched required conceptPath(s): payments-app/payments

**Answer:**

```
payments-app/payments — this subsystem owns the provider abstraction and registry, so adding Braintree means implementing the provider interface and registering it there.
```

### payments-app-2-add-route — PASS ✅

**Question:** Where would I add a new HTTP route, e.g. POST /refunds, that takes a transaction id and refunds it?

- Termination: `final_answer`
- Tokens used: 558
- Concept paths fetched: _(none)_
- Required hits: `payments-app/api`
- Reason: matched required conceptPath(s): payments-app/api

**Answer:**

```
payments-app/api — New HTTP routes (e.g., POST /refunds) belong in the Express HTTP surface subsystem, where checkout and admin endpoints are defined and wired up.
```

### payments-app-3-add-table — PASS ✅

**Question:** Where would I add a new database table — e.g. `Dispute` — to track chargebacks?

- Termination: `final_answer`
- Tokens used: 562
- Concept paths fetched: _(none)_
- Required hits: `payments-app/db`
- Reason: matched required conceptPath(s): payments-app/db

**Answer:**

```
payments-app/db — new database tables (e.g., a `Dispute` model for chargeback tracking) belong in the `db` subsystem, which owns the Prisma schema and Postgres persistence layer.
```

### payments-app-4-change-webhook — PASS ✅

**Question:** Where would I change how outbound webhooks are signed before delivery to merchants?

- Termination: `final_answer`
- Tokens used: 556
- Concept paths fetched: _(none)_
- Required hits: `payments-app/webhooks`
- Reason: matched required conceptPath(s): payments-app/webhooks

**Answer:**

```
payments-app/webhooks — This subsystem handles outbound webhook delivery, making it the correct place to modify how webhooks are signed (e.g., HMAC signature generation or header injection) before they are dispatched to merchants.
```

### payments-app-5-enable-provider — PASS ✅

**Question:** I have already implemented a new provider. Where do I configure which providers are enabled in production?

- Termination: `final_answer`
- Tokens used: 541
- Concept paths fetched: _(none)_
- Required hits: `payments-app/config`
- Reason: matched required conceptPath(s): payments-app/config

**Answer:**

```
payments-app/config — This subsystem handles env-driven configuration including provider keys, making it the right place to configure which providers are enabled in production.
```

