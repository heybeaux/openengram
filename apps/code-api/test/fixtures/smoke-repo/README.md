# smoke-repo

Tiny TypeScript fixture used by `test/cards-api.smoke.e2e-spec.ts` to
verify the v1 cards HTTP API end-to-end against a freshly-indexed repo.

Keep this small (3-5 files) — slower fixtures inflate the smoke runtime.
