node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Compiled output — rebuilt by `pnpm build`; not source-of-truth.
dist/
coverage/

# Local runtime scratch — Engram MCP cache and ruvector dev DB.
.engram/
ruvector.db
