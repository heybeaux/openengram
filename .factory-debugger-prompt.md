You are a factory debugger agent. A worker implemented a ticket but build gates failed.
Your job is to fix the gate failures WITHOUT changing the worker's intended behavior.

TICKET: HEY-417
SUMMARY: Implement PENDING merge resolution — drain the mergeCandidate PENDING queue. Add a dream cycle phase that processes PENDING candidates: auto-merge if similarity >= 0.90, auto-reject if < 0.82, re-evaluate the 0.82-0.90 range with LLM. Track resolution stats. Add DREAM_PENDING_BATCH_SIZE env (default 100). Update lastDreamedAt on processed memories.

WORKTREE PATH: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065

## FAILED GATES

### typecheck (FAILED)
```
src/consolidation/dream-cycle-mutex.spec.ts(63,15): error TS2554: Expected 8-13 arguments, but got 7.
```

### test (FAILED)
```
npm/@nestjs+testing@11.1.12_@nestjs+common@11.1.12_class-transformer@0.5.1_class-validator@0.14.3_hwpqwlvqkuhhy2yj5svz5sq2wq/node_modules/@nestjs/testing/testing-instance-loader.js:9:9)
      at TestingModuleBuilder.createInstancesOfDependencies (../node_modules/.pnpm/@nestjs+testing@11.1.12_@nestjs+common@11.1.12_class-transformer@0.5.1_class-validator@0.14.3_hwpqwlvqkuhhy2yj5svz5sq2wq/node_modules/@nestjs/testing/testing-module.builder.js:118:9)
      at TestingModuleBuilder.compile (../node_modules/.pnpm/@nestjs+testing@11.1.12_@nestjs+common@11.1.12_class-transformer@0.5.1_class-validator@0.14.3_hwpqwlvqkuhhy2yj5svz5sq2wq/node_modules/@nestjs/testing/testing-module.builder.js:74:9)
      at TestingModuleBuilder.builder.compile (test-setup.ts:19:20)
      at Object.<anonymous> (consolidation/dream-cycle.service.spec.ts:130:35)


Test Suites: 1 failed, 193 passed, 194 total
Tests:       10 failed, 2662 passed, 2672 total
Snapshots:   0 total
Time:        28.799 s
Ran all test suites.
```

## WORKER'S CHANGES
```diff
diff --git a/.env.example b/.env.example
index 1d767e5..315b307 100644
--- a/.env.example
+++ b/.env.example
@@ -57,3 +57,4 @@ DIRECT_URL=postgresql://engram:engram@postgres:5432/engram
 # DREAM_STALENESS_SCORE=0.35              # Minimum score to avoid staleness archival
 # DREAM_PATTERN_MIN_SIZE=3                # Minimum cluster size for pattern extraction
 # DREAM_IDENTITY_MAX_MEMORIES=100         # Maximum memories to process for identity consolidation
+# DREAM_PENDING_BATCH_SIZE=100            # Maximum PENDING merge candidates to process per cycle (default 100)
diff --git a/.factory-prompt.md b/.factory-prompt.md
index 9755805..6c6e026 100644
--- a/.factory-prompt.md
+++ b/.factory-prompt.md
@@ -1,36 +1,36 @@
 You are a factory worker agent. Your ONLY job is to implement the following ticket.
 
-TICKET: HEY-415
-SUMMARY: Add dream cycle v2 config env vars
+TICKET: HEY-417
+SUMMARY: Implement PENDING merge resolution — drain the mergeCandidate PENDING queue. Add a dream cycle phase that processes PENDING candidates: auto-merge if similarity >= 0.90, auto-reject if < 0.82, re-evaluate the 0.82-0.90 range with LLM. Track resolution stats. Add DREAM_PENDING_BATCH_SIZE env (default 100). Update lastDreamedAt on processed memories.
 
-WORKTREE PATH: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404
+WORKTREE PATH: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065
 
 SPEC:
-Add dream cycle v2 config env vars
+Implement PENDING merge resolution — drain the mergeCandidate PENDING queue. Add a dream cycle phase that processes PENDING candidates: auto-merge if similarity >= 0.90, auto-reject if < 0.82, re-evaluate the 0.82-0.90 range with LLM. Track resolution stats. Add DREAM_PENDING_BATCH_SIZE env (default 100). Update lastDreamedAt on processed memories.
 
 ## FILE OPERATIONS — CRITICAL
 
-All files you need to modify are in: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404
+All files you need to modify are in: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065
 
-You MUST use the exec tool with workdir="/Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404" for ALL file operations.
+You MUST use the exec tool with workdir="/Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065" for ALL file operations.
 Do NOT use the Write or Edit tools — they are workspace-scoped and cannot reach the worktree.
 
 To read a file:
-  exec: cat /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404/src/some/file.ts
+  exec: cat /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065/src/some/file.ts
 
 To write a file, use a heredoc:
-  exec: cat > /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404/src/some/file.ts << 'ENDOFFILE'
+  exec: cat > /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065/src/some/file.ts << 'ENDOFFILE'
   ... file contents ...
   ENDOFFILE
 
 To make a targeted edit, use sed:
-  exec: sed -i '' 's/oldText/newText/g' /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404/src/some/file.ts
+  exec: sed -i '' 's/oldText/newText/g' /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065/src/some/file.ts
 
 For multi-line edits, prefer writing the complete file with cat/heredoc
 rather than chaining sed commands. It is more reliable.
 
 To verify your changes:
-  exec: cd /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-415-1772404121404 && git diff --stat
+  exec: cd /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065 && git diff --stat
 
 ## RULES
 
@@ -41,4 +41,9 @@ To verify your changes:
 - Write clean, typed code that follows existing patterns in the codebase.
 - If tests exist for modified files, update them.
 - If you are unsure about something, implement the most conservative approach.
-- ALWAYS use exec with the worktree path. NEVER use Write/Edit tools.
\ No newline at end of file
+- ALWAYS use exec with the worktree path. NEVER use Write/Edit tools.
+
+## PREVIOUS ATTEMPT FEEDBACK (fix these issues)
+The verifier found the following issues that must be fixed:
+1. The `catch` block for the 'pending' stage in `src/consolidation/dream-cycle.service.ts` contains an incomplete statement: `const msg = ;`. This will cause a runtime error and needs to be completed with an appropriate error message string (e.g., `const msg = `Error processing pending stage for user ${userId}: ${err.message || err}`;`).
+Also consider: Ensure the `DreamCyclePendingStage` implementation, which is called by this service, explicitly handles updating the `lastDreamedAt` timestamp on all processed memories (merged, rejected, or LLM-re-evaluated) as specified in the ticket.; Consider adding more detailed logging within the `DreamCyclePendingStage` to indicate how many memories were auto-merged, auto-rejected, and sent for LLM re-evaluation, to provide better visibility into the resolution process.
\ No newline at end of file
diff --git a/src/consolidation/consolidation.module.ts b/src/consolidation/consolidation.module.ts
index 34373a7..8830c85 100644
--- a/src/consolidation/consolidation.module.ts
+++ b/src/consolidation/consolidation.module.ts
@@ -9,6 +9,7 @@ import {
   DreamCyclePatternsStage,
   DreamCycleDriftStage,
   DreamCycleIdentityStage,
+  DreamCyclePendingStage,
 } from './stages';
 import { MemoryModule } from '../memory/memory.module';
 import { LLMModule } from '../llm/llm.module';
@@ -33,6 +34,7 @@ import { IdentityModule } from '../identity/identity.module';
     DreamCycleSchedulerService,
     DreamCycleDedupStage,
     DreamCycleStalenessStage,
+    DreamCyclePendingStage,
     DreamCyclePatternsStage,
     DreamCycleDriftStage,
     DreamCycleIdentityStage,
diff --git a/src/consolidation/dream-cycle.service.ts b/src/consolidation/dream-cycle.service.ts
index df82b20..5eeb302 100644
--- a/src/consolidation/dream-cycle.service.ts
+++ b/src/consolidation/dream-cycle.service.ts
@@ -13,6 +13,7 @@ import {
   DreamCyclePatternsStage,
   DreamCycleDriftStage,
   DreamCycleIdentityStage,
+  DreamCyclePendingStage,
 } from './stages';
 import * as os from 'os';
 
@@ -22,6 +23,7 @@ const DREAM_CYCLE_LOCK_KEY = 294967;
 export type DreamCycleStage =
   | 'dedup'
   | 'staleness'
+  | 'pending'
   | 'patterns'
   | 'clustering'
   | 'drift'
@@ -43,6 +45,7 @@ export interface DreamCycleResult {
   duplicatesMerged: number;
   patternsCreated: number;
   memoriesArchived: number;
+  pendingResolved?: number;
   totalActive: number;
   avgEffectiveScore: number;
   stageDetails: Record<string, any>;
@@ -54,6 +57,7 @@ export interface DreamCycleResult {
 const ALL_STAGES: DreamCycleStage[] = [
   'dedup',
   'staleness',
+  'pending',
   'patterns',
   'clustering',
   'drift',
@@ -71,6 +75,7 @@ export class DreamCycleService {
     private config: ConfigService,
     private dedupStage: DreamCycleDedupStage,
     private stalenessStage: DreamCycleStalenessStage,
+    private pendingStage: DreamCyclePendingStage,
     private patternsStage: DreamCyclePatternsStage,
     private driftStage: DreamCycleDriftStage,
     private identityStage: DreamCycleIdentityStage,
@@ -115,6 +120,7 @@ export class DreamCycleService {
         duplicatesMerged: 0,
         patternsCreated: 0,
         memoriesArchived: 0,
+        pendingResolved: 0,
         totalActive: 0,
         avgEffectiveScore: 0,
         stageDetails: {},
@@ -179,6 +185,10 @@ export class DreamCycleService {
           (s, r) => s + (r.memoriesArchived ?? 0),
           0,
         ),
+        pendingResolved: allResults.reduce(
+          (s, r) => s + (r.pendingResolved ?? 0),
+          0,
+        ),
         llmCallsUsed: allResults.reduce((s, r) => s + (r.llmCallsUsed ?? 0), 0),
         errors: allResults.flatMap((r) => r.errors ?? []),
         users
```

## YOUR TASK

1. Read the error output carefully
2. For each failure, make the minimal fix:
   - Type errors in test mocks → add the missing fields with sensible defaults (null, '', 0)
   - Import errors → add the missing imports
   - Lint errors → fix the specific lint violation
   - Test assertion failures from changed behavior → update the test expectation
3. Do NOT change the worker's implementation logic
4. Do NOT fix anything that isn't directly causing a gate failure

## FILE OPERATIONS

All files are in: /Users/clawdbot/projects/agent-memory/.factory-worktrees/hey-417-1772405830065
Use exec tool with that path for ALL file operations.
Use exec: cat for reading, exec: cat > with heredoc for writing.
Do NOT use Write or Edit tools.

## RULES

- Minimal fixes only. Do not refactor, improve, or clean up anything.
- If a test mock is missing a field, add it with a null/default value.
- If an import is missing, add it.
- If you cannot determine the fix, leave it alone.
- Do NOT touch the worker's primary implementation files unless the error is there.
- NEVER use git commands (no commit, no push).